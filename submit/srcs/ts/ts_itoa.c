
#include "libft.h"

#include <stdio.h>

int	main(void)
{
	int n;

	n = INT_MIN;
	printf("%d\t%s\n", n, ft_itoa(n));
	n = INT_MIN;
	printf("%d\t%s\n", n, ft_itoa(n));
	n = INT_MIN;
	printf("%d\t%s\n", n, ft_itoa(n));
	n = INT_MIN;
	printf("%d\t%s\n", n, ft_itoa(n));

}
